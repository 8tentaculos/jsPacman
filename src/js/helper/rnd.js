/**
 * Return random number between 0 and total less one.
 * @param {number} total - The total number of items.
 * @returns {number} The random number.
 */
const rnd = (total) => Math.floor((Math.random() * total));

export default rnd;
